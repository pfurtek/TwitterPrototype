# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TwitterPrototype' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TwitterPrototype
  pod 'IGListKit', '~> 2.0.0'

  target 'TwitterPrototypeTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'IGListKit', '~> 2.0.0'
  end

  target 'TwitterPrototypeUITests' do
    inherit! :search_paths
    # Pods for testing
    pod 'IGListKit', '~> 2.0.0'
  end

end
